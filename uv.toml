[project]
name = "langlearn-app"
version = "0.1.0"
description = "Lightweight Flask backend for a LangGraph + LangChain assisted language tutor."

[project.dependencies]
Flask = "*"
Flask-SQLAlchemy = "*"
SQLAlchemy = "*"
psycopg[binary] = "*"
python-dotenv = "*"
langchain = "*"
langgraph = "*"
lagsmith = "*"
Werkzeug = "*"

[tool.uv.scripts]
start = "flask run"
